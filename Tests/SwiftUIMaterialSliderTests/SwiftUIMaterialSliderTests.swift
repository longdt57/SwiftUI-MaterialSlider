import XCTest
@testable import SwiftUIMaterialSlider

final class SwiftUIMaterialSliderTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUIMaterialSlider().text, "Hello, World!")
    }
}
